/usr/bin/python -c "import pandas as pd; df = pd.read_parquet('/sharedata/hf/BytedTsinghua-SIA_DAPO-Math-17k/data/dapo-math-17k.parquet').head(10000); print(f'Rows: {len(df)}'); print(df[['prompt', 'reward_model']].head(10).to_string()); print(df['reward_model'].apply(lambda x: x.get('ground_truth', '') if isinstance(x, dict) else '').value_counts().head(10)); print(f'Missing ground_truth: {sum(df[\"reward_model\"].apply(lambda x: not isinstance(x, dict) or \"ground_truth\" not in x))}')"
